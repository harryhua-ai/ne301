# Issue #18 附加分析：用户报告「保存后刷新，画的线丢失，出现横的死的线」根因

日期：2026-09-22 晚 · 结论：**非保存/持久化代码缺陷，为测试流程反复覆盖设备配置所致**。

## 时间线（自动化写入设备 canonical 配置的全部记录）

| 时刻（约） | 触发 | 写入的线 |
|---|---|---|
| 16:19 | curl 往返测试 | 0.3→0.7 @ 0.5（水平） |
| 16:20 | 恢复用户线 | 0.262→0.742 @ 0.325 |
| 17:1x | playwright 验证保存 | 0.279→0.72 @ 0.599（水平） |
| 17:3x | playwright 验证保存 | 0.35→0.649 @ 0.449（水平） |
| 17:4x | playwright 验证保存 | 0.4→0.6 @ 0.55（水平） |
| 18:0x | 收口冒烟保存 | **0.379→0.619 @ 0.499（水平，当前值）** |

用户每保存自己的线，下一轮自动化验证即以水平测试线覆盖；用户刷新后看到的是自动化留下的线——表现为「画的线丢失、出现一根横的死的线」，与代码无关且必然复现。

## 只读取证（2026-09-22 晚）

- GET /config：`{"x1":0.379,"y1":0.499,"x2":0.619,"y2":0.499,...}`（= 最后一次自动化写入值）。
- 全新加载页面渲染坐标：0.379 / 0.499 / 0.619 —— 与服务器逐位一致，读取/渲染链路诚实。

## 已确认的真实缺陷（均已修复）

1. 「重置」产生退化线草稿，固件拒绝无线配置（`invalid_config`）→ 死路；已改为 reset-to-saved。
2. 编辑时双线渲染造成「多出一根黄线」；已改为单线草稿预览。
3. 设备存在 60s+ 的 401/超时波窗口，保存会间歇失败（toast 已显示真实原因）。

## 流程禁令（即刻生效）

自动化真机流程 **禁止对设备配置做任何写入**（包括"验证性"保存与恢复性写入）；行为验证只允许只读 GET/渲染比对，或由用户本人操作。

## 补充取证（2026-09-22 18:00）：为什么修复"始终不生效"——设备 HTTP 缓存策略

| 证据 | 值 |
|---|---|
| `/` 响应头 | `Cache-Control: max-age=86400, public` + `ETag "3f94a132"` |
| 含义 | 浏览器 24 小时内不向设备索要新 index.html，一直运行缓存里的旧 JS |
| 旧 hash 资源回退 | `GET /assets/index-BeDhHToU.js`（已不存在的版本）→ **HTTP 200 + index.html 内容**（SPA fallback），非 404 |
| 当前服务器真实版本 | `index-OeUYPgvI.js`（= HEAD d4aed88a 构建） |
| 当前 canonical 线 | `0.379→0.619 @ 0.499`（最后一次自动化冒烟线，水平） |

**机制**：每次 web 重烧后，用户浏览器在最长 24h 内继续运行旧版 JS（含已修复的 bug）。
今天固件未动、web 推送 6 个版本，用户浏览器多数时间运行的是旧构建 → 用户看到/测到的
是旧 bug（双线、保存失败等），与已推送修复脱节 → 表现为"反复修不好"。

**修复归属**：需要固件静态服务对 `index.html` 下发 `Cache-Control: no-cache`（hash 资源可继续长缓存）
→ 固件域，本轮未动，建议独立 Issue。
**用户侧即时规避**：强刷（Cmd+Shift+R）/ DevTools Disable cache，即可加载当前版本。

## 事件（2026-09-22 20:00）：设备路由表运行期丢失单条路由 → 页面「资源不存在」

- 现象：用户页面报「资源不存在」；`GET /api/v1/apps/line-counting/config` 稳定返回
  `NOT_FOUND "API endpoint not found"`（106/106 次），而同模块 status/stats/events 及其他模块端点全部正常。
- 取证：全量探测源码注册的 21 条 GET 路由——17 条 OK、3 条为参数类正常拒绝、
  **仅 LC config GET 一条路由在运行期路由表中消失**（注册顺序里它是该模块第 1 条，同模块后续路由完好）。
- 排除：源码路由表完整；注册/扩容逻辑（8→…→256 增长 + memcpy 迁移）审查无缺陷；非构建问题（重启前旧 App 同样存在该路由）。
- 结论：**设备运行期 RAM 路由表单条表项丢失/损坏**（启动注册后堆内数据被破坏，或启动注册单点异常）——固件域问题。
- 处置：软重启后路由表重建、/config 恢复（用户配置线完好：0.326,0.552→0.799,0.609）。
  soak 采样器已加 `/config NOT_FOUND` 复发告警（ALARM 行）。
- 待办建议：独立固件 Issue——路由表周期性 dump 比对或注册后自校验，抓破坏源。
